# Copy to .env and edit values for local development.
# .env is gitignored.

# Where per-video data folders live (default: ./data)
DATA_DIR=./data

# Where the API listens (only relevant once a UI is wired up)
API_HOST=127.0.0.1
API_PORT=8765